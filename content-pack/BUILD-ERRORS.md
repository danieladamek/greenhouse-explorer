# BUILD-ERRORS

`scripts/build-content.ts` found 1 error(s). The pack was not modified; fix these and re-run `npm run build:content`.
Uncited prose is never repaired by adding a citation, and an unknown id is never re-pointed — inventing provenance is worse than shipping the error (APP-SPEC §6.1).

- **compounds/teucrin-a/occurrences/0** — taxon_id teucrium-chamaedrys is not in taxa.yaml
